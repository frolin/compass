language: ruby
rvm:
  - 2.2.1
cache: bundler
before_script:
  - "bundle exec rake ar:setup --trace"

script:
  - "bundle exec bundle exec rspec --format documentation --color"

notifications:
  webhooks:
    urls:
      - https://webhooks.gitter.im/e/f3bfcb9e11a3e9b597a2
    on_success: change  # options: [always|never|change] default: always
    on_failure: always  # options: [always|never|change] default: always
    on_start: never     # options: [always|never|change] default: always
